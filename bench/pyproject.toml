[project]
name = "sheet"
version = "0.1.0"
description = "Mini spreadsheet CLI"
requires-python = ">=3.13"
readme = "README.md"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
